# teste-pratico

Para rodar a aplicação, tenha instalado o NodeJS em sua máquina

Para rodar o servidor, Execute
    cd api && npm start

Para executar o app, instale globalmente Expo-CLI:
    npm install expo-cli --global

Depois configure o endereço do servidor em app/src/services/api.js na propriedade baseURL.

Para executar o app:
    cd app && npm start